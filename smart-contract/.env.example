DEPLOYER_PRIVATE_KEY=your_funded_wallet_private_key
CELO_SEPOLIA_RPC_URL=https://forno.celo-sepolia.celo-testnet.org/
BLOCKSCOUT_API_KEY=your_blockscout_api_key
# Optional legacy fallback names:
# ETHERSCAN_API_KEY=your_etherscan_api_key
# CELOSCAN_API_KEY=your_celoscan_api_key

# Comma-separated stable token addresses used by EazeeEscrow deployment setup.
# Recommended: set network-specific lists instead of the shared fallback.
CELO_SEPOLIA_SUPPORTED_TOKENS=
CELO_MAINNET_SUPPORTED_TOKENS=
# Optional fallback if a network-specific list is not set:
EAZEE_SUPPORTED_TOKENS=
